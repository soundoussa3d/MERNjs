//exemple 1
const phone = {
    model: "iPhone 15 Pro",
    wifiOn: false,
    locked: true,
    password: 1234,
    turnOnWifi() {
        this.wifiOn = true;
    },
    open: function (pass) {
        if (this.password === pass) {
            this.locked = false;
        }
    }
}

//exemple2 (getters and setters)
const enhancedPhone = {
    model: "iPhone 15 Pro",
    _password: 1234, // Private property
    locked: true,

    // Getter method for password
    get password() {
        return "****";
    },

    // Setter method for password
    set password(newPassword) {
        if (newPassword >= 1000 && newPassword <= 9999) {
            this._password = newPassword;
            console.log('Password updated successfully.');
        } else {
            console.log('Invalid password. Please enter a 4-digit number.');
        }
    },
};


//private property 
we use to hide the real property an underscore before the name.


//classes 
class Phone {
    constructor(model, quantity, price){
        this.model = model;
        this.quantity = quantity;
        this.price = price;
    }
    updateQuantity(newQuantity){
        this.quantity = newQuantity;
    } 
}

const iphone15 = new Phone("iPhone 15 Pro", 20, 999);
let pixel7 = new Phone("Google Pixel 7a", 4, 499);

iphone15.model // "iPhone 15 Pro"
pixel7.updateQuantity(200) // this will update the quantity of pixel7 phones to 200.